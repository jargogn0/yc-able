web: export GOMP=$(find /usr/lib -name 'libgomp.so.1' 2>/dev/null | head -1); export LD_LIBRARY_PATH=$(dirname ${GOMP:-/usr/lib/x86_64-linux-gnu/libgomp.so.1}):$LD_LIBRARY_PATH && uvicorn server:app --host 0.0.0.0 --port $PORT
